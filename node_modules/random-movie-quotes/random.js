var quotes = require('./quotes')

function getQuote () {
  var total = quotes.length
  var rand = Math.floor(Math.random() * (total - 0) + 0)
  return quotes[rand]
}
module.exports.getQuote = getQuote
